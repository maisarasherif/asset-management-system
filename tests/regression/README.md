# AMS Regression Suite

This folder keeps the maintained regression assets together:

- `api/` contains Newman/Postman API collections.
- `e2e/` contains Playwright browser/API specs.
- `fixtures/` contains files used by regression collections.
- `support/` contains local helper servers and targeted runner scripts.
- `ISOLATED_TESTING_GUIDE.md` explains how to run isolated Go, Newman, and Playwright tests for any feature.
- `FEDORA_STAGING_TESTING_GUIDE.md` explains how to run the same isolated suite on Fedora 44 KDE staging.

Together they cover auth/session handling, user permissions, catalog/template CRUD, assets/components/certificates, uploads/reporting, routine maintenance, client portal access, single-asset equipment, admin/client-access screens, competencies, equipment types, scheduler audit APIs, HR/Admin product workflows, and whole-app route health.

## Collections

```text
system-api-smoke.postman_collection.json
admin-surface-regression.postman_collection.json
routine-maintenance.postman_collection.json
client-asset-certificates.postman_collection.json
single-asset-equipment.postman_collection.json
hr-admin-product.postman_collection.json
fixtures/sample-certificate.pdf
fixtures/oversize-certificate.pdf (generated by the isolated runner)
```

Do not add old manual or legacy collections to the automated VPS suite until they are checked against the current API contract.

The isolated runner generates `fixtures/oversize-certificate.pdf` before Newman runs. It is intentionally not committed because it is only used to verify that certificate uploads over 10 MB are rejected.

## Run Via Isolated VPS Runner

From the repository root:

```bash
bash tests/regression/run-vps-isolated-tests.sh
```

API-only:

```bash
RUN_PLAYWRIGHT=0 bash tests/regression/run-vps-isolated-tests.sh
```

One collection:

```bash
RUN_PLAYWRIGHT=0 \
NEWMAN_COLLECTIONS="tests/regression/api/system-api-smoke.postman_collection.json" \
bash tests/regression/run-vps-isolated-tests.sh
```

By default, the isolated runner includes the HR/Admin Newman collection and the HR/Admin Playwright spec. Override `NEWMAN_COLLECTIONS` or `E2E_SPECS` only for targeted diagnosis.

The runner injects:

```text
baseUrl
adminEmail
adminPassword
```

from the isolated API and `ams-server/.env`, so separate Postman environment files are not required for the automated VPS flow.
